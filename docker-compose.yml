# docker-compose.yml
version: '3.8'

services:
  web:
    build: .
    ports:
      # --- ¡CAMBIO AQUÍ! ---
      - "0.0.0.0:8080:8080"
    volumes:
      - reminders-db-data:/app/data
      # --- ¡AÑADE ESTA LÍNEA! ---
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - FLASK_ENV=production
      - DATABASE_PATH=/app/data/reminders.db
      
      # --- Variables de Telegram ---
      # (Asegúrate de que estén rellenas)
      - TELEGRAM_BOT_TOKEN=8109216707:AAFm4OzFjNRcHdiosrLfA_Hv0FSucB5B0EU
      - TELEGRAM_CHAT_ID=730910001

volumes:
  reminders-db-data:


